<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Happy Birthday Princee</title>
  <style>
    :root{
      --bg1:#ffe5f2;
      --bg2:#d3efff;
      --accent:#ff2d7a;
      --text:#555;
    }
    *{box-sizing:border-box}
    html,body{
      height:100%;
      margin:0;
      padding:0;
      font-family: "Poppins", system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
      background: linear-gradient(135deg, var(--bg1), var(--bg2));
      display:flex;
      align-items:center;
      justify-content:center;
    }

    .card{
      background:#fff;
      width:90%;
      max-width:480px;
      padding:30px;
      border-radius:24px;
      box-shadow: 0 18px 40px rgba(12,20,40,0.12);
      text-align:center;
      position:relative;
      overflow:hidden;
    }

    .balloons{
      position:absolute;
      top:-30px;
      left:50%;
      transform:translateX(-50%);
      width:140px;
      opacity:0.98;
      animation: float 3.6s ease-in-out infinite;
      pointer-events:none;
    }
    @keyframes float{
      0%   { transform: translate(-50%, 0) rotate(-2deg); }
      50%  { transform: translate(-50%, -18px) rotate(2deg); }
      100% { transform: translate(-50%, 0) rotate(-2deg); }
    }

    h1{
      font-size:2.1rem;
      margin:24px 0 8px;
      color:var(--accent);
      letter-spacing:0.4px;
      font-weight:700;
    }

    .msg{
      font-size:1.05rem;
      color:var(--text);
      margin:6px 0 0;
      line-height:1.4;
    }

    /* small responsive tweak */
    @media (max-width:420px){
      h1{font-size:1.6rem}
      .balloons{width:110px}
    }
  </style>
</head>
<body>
  <div class="card">
    <img class="balloons" src="https://cdn-icons-png.flaticon.com/512/742/742751.png" alt="balloons" />
    <h1>Happy Birthday <span style="color:var(--accent);">Princee</span> 🎀</h1>
    <p class="msg">May your special day be as cute, bright and magical as you are 🎉💖</p>
  </div>
</body>
</html>